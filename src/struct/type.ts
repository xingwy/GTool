export class Type {
    public name: string;
    public type: string;
    public des: string;

    constructor(name: string, type: string, des: string) {
        this.name = name;
        this.type = type;
        this.des = des;
    }
}